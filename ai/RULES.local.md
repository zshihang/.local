# Local Rules Overrides

## Environment & Tooling

- **Primary Machine**: linux (gLinux)
- **VCS Priority**: always use `jj` over `git` or `hg` when a `.jj` directory is present.
- **Hook Strategy**: use `google3cleaner.sh` for all google3 files to ensure g4/jj compatibility.
- **Custom Projects**: prioritize context for `wsupp-cirqserver` when discussed.

## Behavioral Preferences

- **Concise communication**: sacrifice grammar for speed as requested.
- **Identity**: address the user as "Shihang".
- **Tooling**: prefer modern alternatives (`eza`, `bat`, `fd`, `rg`).
- **Google3**: when working in google3, strictly follow the "superpowers" orchestration protocol (Research -> Strategy -> Execution).
