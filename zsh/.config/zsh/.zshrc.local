export host=ckg.c.googlers.com

# quick work ssh (using official go/glinux/roadwarrior)
# -t starts/attaches tmux; -r skips gcert if >1hr remains
alias swork='rw -t -r ckg'
alias gswork='rw --gmosh -t -r ckg'
